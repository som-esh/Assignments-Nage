In case consul fails to start due to permission denied, just run

sudo chown -R 100:100 ./backend/docker
sudo chmod -R 755 ./backend/docker

################--Run Project--#################
docker compose build
docker compose up
cd frontend && npm install && npm run dev
################################################